import Register from '../components/Register';

const RegisterPage = () => {
  return (
    <div className="page-container">
      <Register />
    </div>
  );
};

export default RegisterPage;
